use crate::get_input;
use itertools::Itertools;

fn read_input() {}

pub fn parta() -> usize {
    0
}
pub fn partb() -> usize {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_parta() {
        assert_eq!(0, parta());
    }

    #[test]
    fn test_partb() {
        assert_eq!(0, partb());
    }
}
